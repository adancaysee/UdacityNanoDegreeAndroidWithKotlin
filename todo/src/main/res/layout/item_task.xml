<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="task"
            type="com.udacity.todo.domain.Task" />

        <variable
            name="onClickListener"
            type="com.udacity.todo.tasklist.TasksAdapter.OnClickListener" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="?android:attr/listPreferredItemHeight"
        android:onClick="@{()-> onClickListener.openTask(task.id)}"
        android:orientation="horizontal"
        android:paddingStart="@dimen/horizontal_margin"
        android:paddingTop="@dimen/list_item_padding"
        android:paddingEnd="@dimen/horizontal_margin"
        android:paddingBottom="@dimen/list_item_padding">

        <CheckBox
            android:id="@+id/complete_checkbox"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:checked="@{task.isCompleted}"
            android:onCheckedChanged="@{(buttonView,isChecked)-> onClickListener.changeTaskActivateStatus(task,isChecked)}" />

        <TextView
            android:id="@+id/title_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginStart="@dimen/horizontal_margin"
            android:text="@{task.titleForList}"
            app:completedTask="@{task.isCompleted}"
            android:textAppearance="@style/TextAppearance.AppCompat.Title" />
    </LinearLayout>
</layout>