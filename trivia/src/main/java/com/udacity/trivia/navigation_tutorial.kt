package com.udacity.trivia

/**
 * popUp is used --> when a new fragment is added->if I use pop up to A fragment firstly remove previous
   fragments until A fragment and after added new fragment
 *
 * When we use menus, we usually navigate directly a destinations rather than actions. (Menu item id == Destination Id)

 */