package com.udacity.todo.tutorial

/**
 * 1.Dependency Injection --> providing the dependencies
 *                        --> I should provide dependencies(ex:the data sources for repository) instead of hardcoding
 * 2.Injection Types
 *   Constructor injection (prefered)
 *   Setter injection
 *
 */