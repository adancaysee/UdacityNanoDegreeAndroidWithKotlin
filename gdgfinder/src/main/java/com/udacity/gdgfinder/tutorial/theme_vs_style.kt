package com.udacity.gdgfinder.tutorial

/**
 * Style
  Map<view attribute, resource> --> key-value stores
  Collection of view attributes
  Associated with single a view
  Usage -- it can be used from any individual view
  Scope -- A style applied to a view only applies to that view, not to any of its children
  Theme
  Map<theme attribute, resource> --> key-value stores
  Not: Theme attributes are different from view attributes because they’re not properties specific to an individual view type
  Collection of named resources
  Associated with context (Application,activity or view/viewgroups)
  Overlay ancestors
  useful broadly across an app -- all ap
  Scope -- A Theme is accessed as a property of a Context and can be obtained from any object which is or has a Context
 *
 *
 * */