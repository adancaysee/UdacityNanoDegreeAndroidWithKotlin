package com.udacity.devbyteviewer.tutorial

/**
 * For long running task with WorkManager
 * Perform task when the app is in background
 * Use constraints (for battery life and data cost)
 *
 * Required Classes
 * Worker (We use CoroutineWorker)
 * WorkRequest -- (OneTimeWorkRequest, PeriodicWorkRequest) -- WorkManager schedule works with WorkRequest
 * WorkManager
 */